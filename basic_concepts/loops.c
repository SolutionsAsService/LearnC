#include <stdio.h>

// This program demonstrates a simple for loop
int main() {
    // Loop from 0 to 4
    for (int i = 0; i < 5; i++) {
        printf("Iteration number: %d\n", i);  // Print the current iteration index
    }

    return 0;
}
